# Issue Tracker

项目尚未接入远程 Issue 时，使用 `.scratch/issues/` 下的本地 Markdown 文件记录任务。

每个 Issue 至少包含：目标、输入、范围、非目标、验收条件、验证命令或人工步骤、依赖和状态。按用户价值拆纵向切片，避免按数据库、API、前端拆成无法独立验收的横向任务。

`.scratch/` 是本地工作区，不作为方法论文档的一部分；需要长期保留的决策应写入 `docs/`、`workflows/` 或 `references/`。
